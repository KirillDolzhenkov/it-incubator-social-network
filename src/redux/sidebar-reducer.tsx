import {ActionType, SidebarPageType} from "./store";

const initialState = {}

const sidebarReducer = (state: SidebarPageType = initialState, action: ActionType) => {
    return state;
}

export {
    sidebarReducer
}